<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="styles/main.css">
    <link rel="shortcut icon" href="images/weather-app.png" type="image/x-icon">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
</head>

<body>
    <div class="container">
        <div class="first">
            <h1 class="heading">Weather App <img src="images/cloudy.png" alt="weather app icon" height="45" width="45">
            </h1>
            <form action="/search" method="post">
                <div class="form">
                    <input name="location" type="text" placeholder="Enter the Location" required>
                    <button type="submit"><span class="material-symbols-outlined">search</span></button>
                </div>
            </form>
        </div>
        <% if (locals.data){ %>
        <div class="second">
            <div class="locationName"><%= data.location.name %>, <%= data.location.region %>, <%= data.location.country %></div>
            <div class="info">
                <div class="image">
                    <img src="images/cloudy.png" height="90" width="90">
                    <div class="weatherDesc"><%= data.current.weather_descriptions %></div>
                </div>
                <div class="weatherInfo">
                    <div class="timezone">Timezone: <%= data.location.timezone_id %></div>
                    <div class="localtime">Time: <%= data.location.localtime %></div>
                    <div class="temp">Temperature: <%= data.current.temperature %>&#x2103;</div>
                    <div class="humidity">Humidity: <%= data.current.humidity %>%</div>
                    <div class="visibility">Visibility: <%= data.current.visibility %></div>
                </div>
            </div>
        </div>
        <% } %>
    </div>
</body>

</html>